server:
  port: 8081   # Change from 8080 to avoid clash

spring:
  application:
    name: order-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      
      spring:
        kafka:
          bootstrap-servers: localhost:9092